'use strict';

class Graphs {

    constructor(initialNode, finalNode, arcId) {
        this.initialNode = initialNode;
        this.finalNode = finalNode;
        this.arcId = arcId;
        this.type = 'Graphs';
    }
}

module.exports = Graphs;
